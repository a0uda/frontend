import { render, screen } from "@testing-library/react";
import App from "./App";
// make the test return 1 if the test is unsuccessful
// dummy test

test("dummy tes", () => {
  expect(1).toBe(1);
});
